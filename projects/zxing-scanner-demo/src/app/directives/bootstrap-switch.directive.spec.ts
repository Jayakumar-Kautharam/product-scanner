import { BootstrapSwitchDirective } from './bootstrap-switch.directive';

describe('BootstrapSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new BootstrapSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
